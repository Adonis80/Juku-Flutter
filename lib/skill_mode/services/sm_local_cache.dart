/// Drift (SQLite) local cache for offline Skill Mode sessions.
/// Wired in SM-1 when offline-first is implemented.
class SmLocalCache {
  // TODO(SM-1): implement drift database for offline SR state
}
