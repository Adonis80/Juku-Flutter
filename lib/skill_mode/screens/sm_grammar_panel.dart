import 'package:flutter/material.dart';

/// Press 3 grammar markup bottom sheet.
/// Full implementation in SM-1.5.
class SmGrammarPanel extends StatelessWidget {
  const SmGrammarPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
