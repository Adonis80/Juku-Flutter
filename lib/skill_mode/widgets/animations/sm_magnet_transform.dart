import 'package:flutter/material.dart';

/// The signature magnet animation — tiles spring from native to foreign word order.
/// Full implementation in SM-1.4. This is the most important widget.
class SmMagnetTransform extends StatelessWidget {
  const SmMagnetTransform({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // Stub — implemented in SM-1.4
  }
}
