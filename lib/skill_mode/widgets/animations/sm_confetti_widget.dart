import 'package:flutter/material.dart';

/// Tile-scoped + full-screen confetti burst.
/// Implemented in SM-2.1.
class SmConfettiWidget extends StatelessWidget {
  const SmConfettiWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
