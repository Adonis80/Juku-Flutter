import 'package:flutter/material.dart';

/// XP orb that flies from tile to HUD XP bar.
/// Implemented in SM-2.1.
class SmXpOrb extends StatelessWidget {
  const SmXpOrb({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
