import 'package:flutter/material.dart';

/// Floating combo counter with shatter effect on break.
/// Implemented in SM-3.1.
class SmComboCounter extends StatelessWidget {
  const SmComboCounter({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
