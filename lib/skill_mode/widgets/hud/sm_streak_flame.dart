import 'package:flutter/material.dart';

/// SVG streak flame that grows with streak days.
/// Implemented in SM-3.1.
class SmStreakFlame extends StatelessWidget {
  const SmStreakFlame({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
