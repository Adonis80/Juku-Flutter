import 'package:flutter/material.dart';

/// Persistent HUD overlay during sessions — XP bar, streak, combo, progress.
/// Implemented in SM-3.1.
class SmHud extends StatelessWidget {
  const SmHud({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
